<% leader ||= f %>
<% id ||= leader.object_id %>
<div class="row">
  <%= leader.input :auto_name, label: false, input_html: {
    data: {
      id: id,
      autocomplete_field: true,
      autocomplete_url: autocomplete_for_leader_people_path(format: :json)
    }
  } %>
</div>
